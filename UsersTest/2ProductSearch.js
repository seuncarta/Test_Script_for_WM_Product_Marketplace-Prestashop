const { Builder, Browser, By, Key, until } = require('selenium-webdriver');
const assert = require("assert");

describe("Product Search", function () {

    it("Search for Product on the marketplace", async function () {


    })

    it("Enter have of the product name and hit the search button", async function () {

    })

    it("Search for product not on the marketplace", async function () {

    })

})

